# UpperBooks Landing Page

Pre-launch credibility page for UpperBooks.

- **Deployed via:** Vercel project `upperbooks-landing` (team `lawyercrjs-projects`)
- **Live at:** https://upperbooks.com
- **Status:** placeholder notify-me form is not yet wired to Supabase

## Notes

This is a static single-file page (`index.html`), intentionally kept separate
from the main `Upper-Books-Project` app repo. No build step — Vercel serves
it as a static site.

Pushes to `main` auto-deploy to production, since this repo is connected
to the Vercel project.
